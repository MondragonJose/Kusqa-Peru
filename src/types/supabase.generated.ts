/**
 * Supabase Database types placeholder.
 *
 * THIS FILE IS A STUB — the real generated types should be created with:
 *   npx supabase gen types typescript --project-id <PROJECT_ID> > src/types/supabase.generated.ts
 *
 * Until regenerated, Database is typed as a permissive record so that
 * createClient<Database>() does NOT restrict which tables the client can query.
 *
 * TYPE BOUNDARY RISK: With this stub, Supabase queries return `any`.
 * Runtime safety is enforced by Zod validation in the repository layer.
 */

export type Database = Record<string, any>;
